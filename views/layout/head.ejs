<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    
    <% if(css) { %>
		<% css.forEach((link)=>{ %>
			 <link rel="stylesheet" href="<%=link %>" /> 
		<% }) %>
    <% } %>
    
    
    <% if(title) { %>
		<title> <%= title %> </title>
    <% } %>

</head>
<body>






